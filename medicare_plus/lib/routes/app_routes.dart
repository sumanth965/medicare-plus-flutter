/// Named route strings used by Navigator and MaterialApp.
/// Named routes make navigation easier to maintain as the app grows.
class AppRoutes {
  const AppRoutes._();

  static const String splash = '/';
  static const String dashboard = '/dashboard';
  static const String login = '/login';
  static const String register = '/register';
}
