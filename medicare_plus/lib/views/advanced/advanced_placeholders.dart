import 'package:flutter/material.dart';import '../../widgets/common.dart';
class AdvancedPlaceholders extends StatelessWidget{const AdvancedPlaceholders({required this.title,super.key});final String title;static Widget reportUpload()=>const AdvancedPlaceholders(title:'Upload Report');@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:Text(title)),body:ListView(padding:const EdgeInsets.all(24),children:[Icon(Icons.construction,size:70,color:Theme.of(c).colorScheme.primary),const SizedBox(height:16),Text('$title placeholder',style:Theme.of(c).textTheme.headlineSmall),const SizedBox(height:8),const Text('Phase 1 placeholder prepared for AI symptom checking, OCR prescriptions, medicine scanning, nearby hospitals, doctor chat, voice assistant, and wearable integration.'),const DisclaimerCard()]));}
