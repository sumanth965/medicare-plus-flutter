import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary & Secondary
  static const Color primary = Color(0xFF3B82F6); // Medical Blue
  static const Color secondary = Color(0xFF10B981); // Emerald Green

  // Background & Surface
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;

  // Semantic
  static const Color success = Colors.green;
  static const Color warning = Colors.orange;
  static const Color error = Colors.red;

  // Dark Theme
  static const Color darkBackground = Color(0xFF111827); // Dark Navy
  static const Color darkSurface = Color(0xFF1F2937); // Dark Grey
  static const Color darkPrimary = Color(0xFF60A5FA); // Blue Accent
  static const Color darkSecondary = Color(0xFF34D399);

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color darkTextPrimary = Color(0xFFF8FAFC);
  static const Color darkTextSecondary = Color(0xFF94A3B8);
}
